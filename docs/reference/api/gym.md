# Gym interop

Vectorized [Gymnasium](https://gymnasium.farama.org)-compatible environments
over DynaPlex MDPs — see the
[user guide](../../training/gym-environments.md). Requires the optional
`gymnasium` dependency (`pip install "dynaplex[gym]"`).

::: dynaplex.gym.VectorEnv
